#include "Vertex.hpp"

Vertex::Vertex(const Vector3& pos) : position(pos) {
    // Simple constructor that initializes the vertex position
    // Additional properties like color, normal, UV coordinates can be added later
}